/**
 * CustomCursor - REMOVED
 *
 * Custom cursors cause performance issues on most devices.
 * Using native cursor for better user experience.
 *
 * If you want to re-enable a custom cursor in the future:
 * 1. Use CSS-only cursor (cursor: url() or unicode characters)
 * 2. Do NOT use requestAnimationFrame loops
 * 3. Do NOT update React state on mouse events
 * 4. Test thoroughly on mobile/touch devices
 */
export default function CustomCursor() {
  return null
}
